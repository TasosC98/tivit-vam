experiment:
  name: pianovam_hdf5_baseline

dataset:
  root_dir: /home/mkoziri/datasets/PianoVAM_v1.0
  preprocessed_format: hdf5
  hdf5_root: /home/mkoziri/datasets/PianoVAM_v1.0/HDF5
  metadata_json: /home/mkoziri/datasets/PianoVAM_v1.0/metadata_v2.json

  # κράτα τα ίδια που ήδη είδες στο smoke (για να μην αλλάξει τίποτα)
  frames: 96
  batch_size: 1
  num_workers: 2
  shuffle: true

training:
  epochs: 40
  eval_freq: 1
  save_every: 1
  log_interval: 10
  amp: false

logging:
  log_dir: logs/pianovam_hdf5_baseline
  checkpoint_dir: logs/pianovam_hdf5_baseline/checkpoints
  tensorboard: false
